[pytest]
# Each app's tests live next to the app (hue_ent/tests, ...) and import it via a
# sys.path insert, so no packaging is needed to run them:
#   python -m pytest
asyncio_mode = strict
addopts = -q
